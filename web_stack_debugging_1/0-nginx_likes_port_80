#!/usr/bin/env bash
#Created the 0-nginx_likes_port_80 script
apt-get update -y && apt-get install -y nginx && sed -i "s/listen 80 default_server;/listen 80;/g" /etc/nginx/sites-enabled/default && nginx -s reload
